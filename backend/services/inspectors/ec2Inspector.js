/**
 * EC2 Inspector Module (Restructured)
 * EC2 서비스에 대한 보안 및 모범 사례 검사
 * Requirements: 2.1, 2.2, 2.3, 2.4
 */

// 새로운 구조화된 EC2 Inspector 사용
const EC2Inspector = require('./ec2/index');

// 새로운 구조화된 EC2 Inspector를 직접 export

module.exports = EC2Inspector;